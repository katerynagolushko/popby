# Supabase — create a free project at https://supabase.com

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# New projects: publishable key (sb_publishable_...). Legacy: anon JWT.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Optional — for Luma screenshot event extraction
OPENAI_API_KEY=sk-...

# Optional — waitlist confirmation email via Resend (signup still works without these)
# RESEND_API_KEY=re_...
# WAITLIST_FROM_EMAIL="Hangbyme <hello@hangby.me>"
# WAITLIST_REPLY_TO=hello@hangby.me

# Map tiles use OpenFreeMap (free, no key). Nothing to configure.
# Authentication → URL Configuration → Redirect URLs:
# https://your-app.vercel.app/auth/callback
# http://localhost:3000/auth/callback
